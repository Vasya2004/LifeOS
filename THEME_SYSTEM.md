# 🎨 LifeOS Theme System v2.0

A/B/C Theme Switcher с тремя вариантами футуристичного дизайна.

---

## 🚀 Быстрый старт

### 1. Демо страница
```bash
npm run dev
# Открой http://localhost:3000/theme-demo
```

### 2. Переключатель темы
```tsx
import { ThemeSwitcher } from "@/components/theme-switcher"

// В header или sidebar
<ThemeSwitcher />
```

### 3. Программное управление
```tsx
import { useTheme } from "@/hooks/use-theme"

const { theme, setTheme } = useTheme()

setTheme("cyan")   // Neon Cyan - футуристичный минимализм
setTheme("indigo") // Electric Indigo - профессиональный
setTheme("glass")  // Glass Morphic - премиум минимализм
```

---

## 🎭 Три темы

### 1. Neon Cyan (по умолчанию)

```
┌─────────────────────────────────────┐
│  #030305 → #0A0A0F  Background      │
│  #22D3EE           Primary (cyan)   │
│  #8B5CF6           Accent (violet)  │
└─────────────────────────────────────┘
```

**Ощущение**: Терминал, sci-fi, cyberpunk-minimal  
**Когда использовать**: Футуристичный продукт, tech-аудитория  
**Особенности**: Яркое cyan свечение, высокий контраст

---

### 2. Electric Indigo

```
┌─────────────────────────────────────┐
│  #0A0A0F → #111118  Background      │
│  #6366F1            Primary (indigo)│
│  #38BDF8            Accent (sky)    │
└─────────────────────────────────────┘
```

**Ощущение**: Профессиональный, доверительный, корпоративный  
**Когда использовать**: B2B продукт, консервативная аудитория  
**Особенности**: Сдержанные glow-эффекты, глубокие тона

---

### 3. Glass Morphic

```
┌─────────────────────────────────────┐
│  #0F0F13 → #1A1A24  Gradient BG     │
│  rgba(255,255,255,0.03)  Cards      │
│  #8B5CF6            Primary (violet)│
│  #FB7185            Accent (rose)   │
└─────────────────────────────────────┘
```

**Ощущение**: Премиум, воздушный, современный (как macOS)  
**Когда использовать**: Премиум продукт, креативная аудитория  
**Особенности**: Blur эффекты, полупрозрачность, мягкие тени

---

## 🛠️ Использование в компонентах

### CSS Переменные

Все компоненты используют CSS переменные и автоматически адаптируются:

```tsx
// Primary цвет текущей темы
<button className="bg-primary text-primary-foreground">
  Primary Button
</button>

// Glow эффекты
<div className="shadow-[0_0_20px_-5px_var(--primary-glow)]">
  Glowing element
</div>

// Семантические цвета
<span className="text-success">Успех</span>
<span className="text-warning">Предупреждение</span>
<span className="text-destructive">Ошибка</span>
```

### Доступные переменные

| Переменная | Описание |
|------------|----------|
| `--background` | Фон приложения |
| `--foreground` | Основной текст |
| `--primary` | Основной цвет темы |
| `--primary-glow` | Цвет свечения |
| `--accent` | Акцентный цвет |
| `--muted` | Приглушённый фон |
| `--border` | Цвет границ |
| `--success` | Успех (зелёный) |
| `--warning` | Предупреждение (жёлтый) |
| `--destructive` | Ошибка (красный) |

---

## 🎨 Кастомизация

### Создать свою тему

1. Добавьте новый блок в `app/globals.css`:

```css
[data-theme="mytheme"] {
  --background: oklch(0.03 0.01 260);
  --foreground: oklch(0.95 0.01 260);
  --primary: oklch(0.65 0.2 280);
  /* ... остальные переменные */
}
```

2. Обновите типы в `hooks/use-theme.ts`:

```ts
type Theme = "cyan" | "indigo" | "glass" | "mytheme"
```

3. Добавьте в `ThemeSwitcher`:

```tsx
const themes: ThemeOption[] = [
  // ... существующие темы
  {
    id: "mytheme",
    name: "My Theme",
    description: "Описание",
    icon: <Icon />,
    color: "#COLOR",
  },
]
```

---

## 📱 Theme-specific стили

### Cyan theme

```css
[data-theme="cyan"] .glow-primary {
  box-shadow: 0 0 var(--glow-blur) var(--primary-glow);
}
```

### Glass theme

```css
[data-theme="glass"] .glass-card {
  background: oklch(1 0 0 / 0.03);
  backdrop-filter: blur(12px);
  border: 1px solid oklch(1 0 0 / 0.08);
}
```

---

## 💾 Сохранение темы

Тема автоматически сохраняется в `localStorage` и применяется при загрузке:

```ts
// При инициализации приложения
const savedTheme = localStorage.getItem("lifeos-theme")
if (savedTheme) {
  document.documentElement.setAttribute("data-theme", savedTheme)
}
```

---

## 🔄 Миграция существующих компонентов

### Было (hardcoded цвета):

```tsx
<button className="bg-indigo-600 text-white hover:bg-indigo-500">
  Save
</button>
```

### Стало (CSS переменные):

```tsx
<Button>Save</Button>
// или
<button className="bg-primary text-primary-foreground hover:brightness-110">
  Save
</button>
```

---

## 📊 Сравнение

| Характеристика | Cyan | Indigo | Glass |
|----------------|------|--------|-------|
 **Яркость** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **Профессионализм** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Уникальность** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Читаемость** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Glow эффекты** | Сильные | Средние | Минимальные |

---

## 🎯 Рекомендации

### Выбирайте Cyan если:
- Хотите выделиться
- Целевая аудитория — молодые технологичные пользователи
- Приложение связано с AI, кодом, технологиями

### Выбирайте Indigo если:
- Нужен корпоративный вид
- B2B продукт
- Хотите классический, доверительный дизайн

### Выбирайте Glass если:
- Премиум сегмент
- Много графиков/визуализаций
- Нужен "воздушный" интерфейс

---

## 🐛 Troubleshooting

### Тема не применяется
1. Проверьте `data-theme` атрибут на `<html>`
2. Убедитесь что CSS переменные определены в `globals.css`
3. Проверьте что компоненты используют CSS переменные, а не hardcoded цвета

### Мигание при загрузке
Добавьте в `head`:
```html
<script>
  // Применить тему до рендера
  const theme = localStorage.getItem("lifeos-theme") || "cyan"
  document.documentElement.setAttribute("data-theme", theme)
</script>
```

---

## 📄 Файлы

| Файл | Описание |
|------|----------|
| `app/globals.css` | CSS переменные всех тем |
| `components/theme-switcher.tsx` | UI переключатель |
| `hooks/use-theme.ts` | React hook |
| `app/theme-demo/page.tsx` | Демо страница |
| `THEME_SYSTEM.md` | Эта документация |
