# 🎨 LifeOS Design System

> Полная система дизайна для приложения LifeOS

## 📦 Что включено

### Компоненты (обновлены)
- ✅ **Button** - 7 вариантов + геймификация (xp, coin, streak)
- ✅ **Card** - 7 вариантов (default, elevated, interactive, color accents)
- ✅ **Badge** - 15+ вариантов для всех случаев
- ✅ **Progress** - XP bar с glow, health, стандартный
- ✅ **Skeleton** - 5 готовых паттернов загрузки

### Токены
- 🎨 **Цвета** - Полная палитра + тёмная тема
- 📏 **Отступы** - 25+ значений
- 🔤 **Типографика** - Шрифты, размеры, веса
- ✨ **Тени** - Elevation + glow эффекты
- 🎬 **Анимации** - Keyframes, transitions

## 🚀 Быстрый старт

### 1. Импорт компонентов

```tsx
// Вместо старого способа
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'

// Используй новый
import { Button, Card, Badge, Progress } from '@/components/ui'
```

### 2. Использование

```tsx
// Кнопка с XP
<Button variant="xp" size="lg">+100 XP</Button>

// Карточка задачи
<Card variant="interactive">
  <CardContent>Кликни меня</CardContent>
</Card>

// Прогресс XP
<XpProgress current={750} max={1000} level={12} />

// Бейдж приоритета
<PriorityBadge priority="high" />
```

## 📖 Документация по компонентам

### Button

| Variant | Описание |
|---------|----------|
| `default` | Основная кнопка (indigo) |
| `secondary` | Вторичная (slate) |
| `outline` | Контурная |
| `ghost` | Прозрачная с ховером |
| `destructive` | Опасное действие (red) |
| `success` | Успех (emerald) |
| `xp` | Геймификация (gradient) |
| `coin` | Монеты (amber) |
| `streak` | Стрик (orange) |

**Sizes:** `xs`, `sm`, `default`, `lg`, `xl`, `icon`

**Props:**
- `loading` - Показать спиннер
- `leftIcon` / `rightIcon` - Иконки

### Card

| Variant | Описание |
|---------|----------|
| `default` | Стандартная |
| `elevated` | С тенью |
| `interactive` | Кликабельная |
| `primary` | Indigo акцент |
| `success` | Зелёный акцент |
| `warning` | Жёлтый акцент |
| `error` | Красный акцент |

**Padding:** `none`, `sm`, `md`, `lg`, `xl`

### Badge

**Gamification:**
- `<XpBadge amount={50} />`
- `<CoinBadge amount={1000} />`
- `<StreakBadge days={7} />`

**Task Priorities:**
- `<PriorityBadge priority="low" />`
- `<PriorityBadge priority="medium" />`
- `<PriorityBadge priority="high" />`
- `<PriorityBadge priority="critical" />`

**Status:**
- `<StatusBadge status="todo" />`
- `<StatusBadge status="progress" />`
- `<StatusBadge status="done" />`

### Progress

```tsx
// XP с glow эффектом
<XpProgress current={750} max={1000} level={12} />

// Здоровье
<HealthProgress current={85} />

// Задачи
<TasksProgress completed={12} total={20} />

// Цель
<GoalProgress current={50000} target={100000} title="Накопления" />
```

### Skeleton

```tsx
// Готовые паттерны
<SkeletonCard />
<SkeletonList count={5} />
<SkeletonStats count={4} />
<SkeletonTable rows={5} cols={4} />
<SkeletonProfile />

// Кастомный
<Skeleton className="h-4 w-[200px]" />
<Skeleton className="h-32 w-full" shimmer />
```

## 🎨 Демо страница

Открой `/design-system-demo` чтобы увидеть все компоненты в действии.

```bash
npm run dev
# Открой http://localhost:3000/design-system-demo
```

## 🔄 Миграция существующего кода

### Было:
```tsx
<button className="bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-500">
  Сохранить
</button>
```

### Стало:
```tsx
<Button>Сохранить</Button>
```

### Было:
```tsx
<div className="bg-slate-900 rounded-xl border border-slate-800 p-6">
  <h3 className="text-xl font-semibold">Заголовок</h3>
  <p className="text-slate-400">Текст</p>
</div>
```

### Стало:
```tsx
<Card>
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
  </CardHeader>
  <CardContent>
    <p className="text-slate-400">Текст</p>
  </CardContent>
</Card>
```

## 📁 Структура файлов

```
components/ui/
├── index.ts           # Единая точка входа
├── button.tsx         # Кнопки (+ gamification)
├── card.tsx           # Карточки
├── badge.tsx          # Бейджи (+ gamification)
├── progress.tsx       # Прогресс-бары (+ gamification)
├── skeleton.tsx       # Скелетоны загрузки
└── ...                # Остальные shadcn компоненты

design-system/
├── tokens.ts          # Цвета, отступы, типографика
├── components.ts      # Готовые стили (CSS классы)
├── example-usage.tsx  # Примеры компонентов
├── README.md          # Документация
└── SETUP.md           # Быстрая настройка
```

## 🎯 Чеклист внедрения

- [x] Обновить Button
- [x] Обновить Card
- [x] Обновить Badge
- [x] Обновить Progress
- [x] Создать Skeleton
- [ ] Заменить кнопки в Dashboard
- [ ] Заменить кнопки в Tasks
- [ ] Заменить кнопки в Habits
- [ ] Заменить карточки в Goals
- [ ] Добавить Skeleton в async компоненты
- [ ] Добавить Empty states

## 💡 Лучшие практики

1. **Всегда используй компоненты из `@/components/ui`**
   - Не пиши CSS вручную
   - Не дублируй классы

2. **Для геймификации используй специальные варианты:**
   - `variant="xp"` для XP действий
   - `variant="coin"` для монет
   - `variant="streak"` для стриков

3. **Для загрузки используй Skeleton:**
   - Показывай сразу при загрузке данных
   - Используй `shimmer` для привлечения внимания

4. **Для прогресса используй XpProgress:**
   - Автоматически показывает уровень
   - Имеет glow эффект
   - Анимированный

## 🆘 Помощь

Если что-то не работает:
1. Проверь импорт из `@/components/ui`
2. Убедись что `cn()` утилита работает
3. Проверь что Tailwind настроен

## 📝 Changelog

### v1.0.0
- ✅ Обновлён Button с gamification вариантами
- ✅ Обновлён Card с цветными акцентами
- ✅ Обновлён Badge с 15+ вариантами
- ✅ Создан Progress с XP glow
- ✅ Создан Skeleton с 5 паттернами
- ✅ Создана демо страница
